# Aurora UI

The Aurora design system. React components for our product surfaces.

## Install

Ask #design-system for access.

## Use

```tsx
import { Button, Card } from 'aurora-ui'
```

Colours, spacing and type are defined in `src/styles.css`. Ask a designer if you're
unsure which to use.
